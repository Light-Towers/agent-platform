# deepagents 生产化依赖清单（与 pyproject.toml [project].dependencies 对齐）
#
# 本文件仅列「硬依赖」（最小可运行集）。可选能力（链路追踪/缓存/文档生成/Excel）
# 通过 pyproject [project.optional-dependencies] 安装，避免一股脑装无用包：
#   pip install -e "./deepagents[observability,cache,docs,excel]"
#
# 使用 uv 管理的联邦网关请直接依赖 pyproject.toml（本文件为非 uv 用户备选）。

# --- 本地 editable 包 ---
-e ../agent-core
-e ../shared-schemas

# --- SQL 守卫（deepagents 委托 agent_core 的 mysql dialect 守卫，需 sqlglot 透传） ---
sqlglot>=25.0

# --- 核心框架 ---
deepagents==0.7.5
langchain>=1.3.14
langchain-core>=1.5.3
langchain-openai>=1.4
langgraph>=1.2.10
langgraph-checkpoint>=4.2.0

# --- Web 框架 ---
fastapi>=0.129
uvicorn>=0.41
python-dotenv>=1.2
python-multipart>=0.0.9

# --- LLM / 数据 ---
pydantic>=2.13
pydantic-settings>=2.14
pyyaml>=6.0
httpx>=0.28
requests>=2.34.2
numpy>=2.0.0
ragflow-sdk>=0.22.1

# --- 数据库 / SQL ---
pymysql>=1.1.0
SQLAlchemy>=2.0.46
sqlparse>=0.5

# --- 搜索 ---
tavily-python>=0.7

# --- 韧性 ---
tenacity>=9.1
