# Agent Platform 任务进展 Handoff（2026-08-21，第 2 次更新）

> 供下一个 agent session 接续工作使用。本文档总结已完成的任务、验证状态、剩余待办，以及关键文件路径。
>
> **第 2 次更新（2026-08-21 下午）**：后续 session 已落地 P2-2 / P3-1 / P3-2 / P4-1 / P5-1 及 §20（Workflow DSL / UnifiedPlanner / PG durability），本文档已同步。

## 一、项目概况

- **仓库**：`d:\Study\github\agent-platform`（uv workspace monorepo）
- **架构**：单 Runtime + 多 Planner（Plan-F）；`packages/agent-core`（零依赖内核）+ `packages/agent-runtime`（运行时中间件）+ `packages/shared-schemas`（联邦契约）+ `applications/`（6 个应用）
- **门禁命令**：`make ci` = lint + 根 pytest + 启发式 eval

## 二、本轮完成的任务（2026-08-21）

### 代码实施

| # | 任务 | 改动文件 | 说明 |
|---|------|----------|------|
| 1 | **P4-3** coalesce 诚实改名 | `coordinator.py` / `schemas.py` / `agent_server/config.py` | coalesce 从策略枚举移除，仅保留 queue/reject 二策略 |
| 2 | **TD-10** admission 状态枚举常量 | `agent_runtime/schemas.py` / `admission.py` / `agent_server/api/routes.py` | 新增 `ADMISSION_ADMITTED/QUEUED/REJECTED` 常量，替代散点硬编码字符串；修复 SQL 语法错误 |
| 3 | **P4-2** 收紧 registry.execute 可见性 | 新增 `scripts/lint_architecture.py` + `Makefile` | 架构约束 lint：白名单外禁止 `registry.execute()` 直接调用 |
| 4 | **P2-1** tokens/cost 聚合器 | `agent_runtime/planner/protocol.py` | `ExecutionContext` 新增 `tokens_used/cost_used/max_tokens/max_cost` + `record_usage()` 方法；`PlannerRuntime` 透传配置 |
| 5 | **TD-7** 路由特征词外置 | 新增 `agent_server/agent/data/route_hints.json` + `router.py` | `SQL_HINTS/RAG_HINTS` 等从硬编码改为 JSON 数据文件 + `@lru_cache` |
| 6 | **TD-8** longterm prompt 泛化 | `agent_server/memory/longterm.py` | 示例 `"用户是财务，偏好简洁报表"` → 中性模板 `<用户偏好或事实的中性描述>` |
| 7 | **TD-9** eval 超参统一 | `zhanggui-zhiku/eval/run_eval.py` | `_RUNTIME_BASELINE` 从硬编码改为读 `retrieval_cfg` / `rerank_cfg` |
| 8 | **TD-5** 商品名确认阈值参数化 | `zhanggui-zhiku/.../node_item_name_confirm.py` | `0.85/0.6` 改为环境变量 `ITEM_CONFIRM_HIGH_THRESHOLD` / `ITEM_CONFIRM_MID_THRESHOLD` |
| 9 | **TD-3/TD-4** 文档标记 | `docs/tech-debt/tech-debt-hardcoded-logic.md` | 标记为由 WS-6 统一意图架构隐式解决 |
| 10 | **P2-2** llm client 计量点 | `agent_core/llm/fallback.py` / `fallback_lc.py` / `agent_runtime/planner/protocol.py` / `execution_graph.py` | 计量源 `FallbackChatModel` 抽取 `usage_metadata`→`on_usage` 回调；`PlannerRuntime` 接线到 `ExecutionContext.record_usage`（contextvars 隔离）；`status` 事件带累计 `tokens_used`/`cost_used` |

### 文档更新

| 文件 | 更新内容 |
|------|----------|
| `docs/tech-debt/tech-debt-hardcoded-logic.md` | TD-1~TD-14 全部标记 ✅ 已修复（TD-1 含回归验证） |
| `docs/operations/runtime-governance-roadmap.md` | P2-1 / P2-2 / P3-1 / P3-2 / P4-1 / P4-2 / P4-3 / P5-1 **全部标记 ✅ 已落地**（第 2 次更新补齐 P3-1/P3-2/P5-1） |

### 后续 session 新增落地（2026-08-21）

| # | 任务 | 改动文件 | 说明 |
|---|------|----------|------|
| 10 | **P2-2** llm client 计量点 | `agent_core/llm/fallback.py` / `fallback_lc.py` / `planner/protocol.py` / `execution_graph.py` | 计量源抽取 `usage_metadata` → `on_usage` 回调 → PlannerRuntime 接线 `ExecutionContext.record_usage`；`status` 事件带 tokens/cost |
| 11 | **P3-1** Trajectory 持久化 | 新增 `agent_runtime/trajectory/`（models/store/store_pg）+ `execution_graph._persist_trajectory` + `main.py` 装配 `PgTrajectoryStore` | 按 execution_id 持久化完整轨迹（steps/plan/snapshot/tokens/cost） |
| 12 | **P3-2** Trajectory Replay | `trajectory/replay.py` | `replay_trajectory` 复用 execute_plan 真实链，报告五类 divergence |
| 13 | **P4-1** 分布式 session lease | `coordinator.py` | 可插拔 `LeaseBackend`（InMemory 默认 / PgAdvisory 多副本）+ TTL 过期 + 双写 |
| 14 | **P5-1** TrajectoryFingerprint | `protocol.py` | `_fingerprint` 语义循环检测，`enable_loop_fingerprint` 默认关闭 |
| 15 | **§20** Workflow DSL + UnifiedPlanner + PG durability | `skills/workflow.py` / `planners/unified.py` / `planner/durability_pg.py` | YAML 声明式 Workflow 编译为 Skill；graph/workflow 统一分发；PG 持久化套件（已提交 724a8a9 等） |
| 16 | **TD-1** kefu 意图路由验证 | `kefu_agent/graph.py` | 确认真实复用 `agent_core.intent`（classify_l1/l2_judge/is_chitchat），回归测试锁定 |

### 前几轮已完成（上下文摘要）

- **WS-1~WS-8**（2026-08-20）：Agent 核心架构优化八工作流全量落地（MemoryStore 统一门面 / EventBus / KernelConfig / CircuitBreaker 并发安全 / FallbackChatModel 组合熔断 / GuardMiddleware / LLM LRU 缓存 / 意图分类器数据化）
- **Plan-F Phase 0~3**（2026-08-19）：Capability→Skill rename / 运行时模块迁入 agent-runtime / Planner 协议 / 组合治理 / WS 出口统一
- **架构审核落地**（2026-08-19）：PlannerRuntime per-request 隔离 / Skill 入参契约校验 / 术语精确化
- **文档更新**（2026-08-20）：CHANGELOG / ARCHITECTURE / AGENTS / README 等 6 个文档同步

## 三、验证状态

| 门禁 | 结果 |
|------|------|
| `ruff check .` | ✅ All checks passed（本轮修复 2 处 import 排序） |
| `scripts/lint_architecture.py` | ✅ P4-2 架构约束通过 |
| 根 pytest（tests + agent-core/tests，原 import 失败目录已修复） | ✅ **541 passed** |
| 联邦 unit tests | ✅ **92 passed** |
| kefu tests | ✅ **8 passed**（耗时约 1 小时，套件内含长耗时用例，可优化） |
| eval（启发式） | ✅ **12/12 = 100%**（本轮实际运行） |
| 专项：P2-2 / P3-1 / P3-2 / P5-1 / session-lease | ✅ **36 passed**（五个专项测试文件合计） |

> **注意**：`tests/dialogue_framework/` 与 `tests/wenda_data_agent/` 的 import 失败问题已在后续 session 修复，`uv run pytest -q` 全量采集零错误。

## 四、剩余待办（按优先级）

### 代码侧：无未落地项

runtime-governance-roadmap 全区段闭环（P0/P1/P2/P3/P4/P5 均 ✅）；tech-debt TD-1~TD-14 全部 ✅ 且 TD-1 已有回归验证（kefu 8 passed 锁定）。
### 待人工确认（代码侧无可推进项）

| # | 事项 | 来源 | 阻塞原因 |
|---|------|------|----------|
| 1 | ADR-0004 复评 | `docs/adr/0004-typed-memory-kernel.md` 状态「修订中（Revised，待复评）」 | 需人工评审后变 Accepted |
| 2 | research-proposal 固化 | `docs/architecture/research-proposal.md` 状态「方案稿，待人工确认后固化」 | 需人工确认 |
| 3 | 智能阈值基线采集 | tech-debt TD-5/TD-6 备注 | 参数化已完成，需真实流量基线后评估 |

### 工作区未提交变更

`git status` 有未提交修改（`main.py` / `db.py` / `durability_pg.py` / `trajectory/__init__.py` + 各测试小幅调整，及新文件 `trajectory/store_pg.py`），均为 PG durability 收尾；门禁全绿，可提交（需用户确认）。

### 可选优化（非阻塞）

- kefu 套件耗时约 1 小时，可定位长耗时用例优化
- `enable_loop_fingerprint` 在生产灰度评估开启时机

## 五、关键文件索引

| 用途 | 路径 |
|------|------|
| 技术债清单 | `docs/tech-debt/tech-debt-hardcoded-logic.md`（TD-1~TD-14，全部已修复） |
| Runtime 路线图 | `docs/operations/runtime-governance-roadmap.md`（P2-1~P5-1 **全部已落地**） |
| 架构改进计划 | `docs/architecture/architecture-improvement-plan.md`（WS-1~WS-8 全量落地） |
| Plan-F 设计文档 | `docs/plans/plan-f-single-runtime-multi-planner.md` |
| 架构约束 lint | `scripts/lint_architecture.py` |
| ExecutionContext（P2-1/P2-2/P5-1） | `packages/agent-runtime/agent_runtime/planner/protocol.py` |
| admission 常量（TD-10） | `packages/agent-runtime/agent_runtime/schemas.py` |
| Trajectory 持久化/回放（P3-1/P3-2） | `packages/agent-runtime/agent_runtime/trajectory/` |
| 会话 lease（P4-1） | `packages/agent-runtime/agent_runtime/coordinator.py` |
| 路由特征词数据（TD-7） | `applications/agent_server/agent/data/route_hints.json` |
| CHANGELOG | `CHANGELOG.md` |

## 六、建议的下一个 agent 执行顺序

1. **提交工作区变更**：PG durability 收尾修改已验证全绿（需用户确认后 commit）
2. **ADR-0004 复评材料准备**：整理 typed-memory-kernel 修订点供人工评审
3. **可选优化**：kefu 套件长耗时用例定位；`enable_loop_fingerprint` 生产灰度评估

## 七、建议 Skills

| Skill | 用途 |
|-------|------|
| `/code-review` | 对本轮所有变更做一次完整 code review |
| `/verification-before-completion` | 在声称任务完成前运行完整门禁验证 |
| `/implement` | 按后续 spec 实施新需求 |
