# Agent Platform 任务进展 Handoff（2026-08-21）

> 供下一个 agent session 接续工作使用。本文档总结本轮（及前几轮）已完成的任务、验证状态、剩余待办，以及关键文件路径。

## 一、项目概况

- **仓库**：`d:\Study\github\agent-platform`（uv workspace monorepo）
- **架构**：单 Runtime + 多 Planner（Plan-F）；`packages/agent-core`（零依赖内核）+ `packages/agent-runtime`（运行时中间件）+ `packages/shared-schemas`（联邦契约）+ `applications/`（6 个应用）
- **门禁命令**：`make ci` = lint + 根 pytest + 启发式 eval

## 二、本轮完成的任务（2026-08-21）

### 代码实施

| # | 任务 | 改动文件 | 说明 |
|---|------|----------|------|
| 1 | **P4-3** coalesce 诚实改名 | `coordinator.py` / `schemas.py` / `agent_server/config.py` | coalesce 从策略枚举移除，仅保留 queue/reject 二策略 |
| 2 | **TD-10** admission 状态枚举常量 | `agent_runtime/schemas.py` / `admission.py` / `agent_server/api/routes.py` | 新增 `ADMISSION_ADMITTED/QUEUED/REJECTED` 常量，替代散点硬编码字符串；修复 SQL 语法错误 |
| 3 | **P4-2** 收紧 registry.execute 可见性 | 新增 `scripts/lint_architecture.py` + `Makefile` | 架构约束 lint：白名单外禁止 `registry.execute()` 直接调用 |
| 4 | **P2-1** tokens/cost 聚合器 | `agent_runtime/planner/protocol.py` | `ExecutionContext` 新增 `tokens_used/cost_used/max_tokens/max_cost` + `record_usage()` 方法；`PlannerRuntime` 透传配置 |
| 5 | **TD-7** 路由特征词外置 | 新增 `agent_server/agent/data/route_hints.json` + `router.py` | `SQL_HINTS/RAG_HINTS` 等从硬编码改为 JSON 数据文件 + `@lru_cache` |
| 6 | **TD-8** longterm prompt 泛化 | `agent_server/memory/longterm.py` | 示例 `"用户是财务，偏好简洁报表"` → 中性模板 `<用户偏好或事实的中性描述>` |
| 7 | **TD-9** eval 超参统一 | `zhanggui-zhiku/eval/run_eval.py` | `_RUNTIME_BASELINE` 从硬编码改为读 `retrieval_cfg` / `rerank_cfg` |
| 8 | **TD-5** 商品名确认阈值参数化 | `zhanggui-zhiku/.../node_item_name_confirm.py` | `0.85/0.6` 改为环境变量 `ITEM_CONFIRM_HIGH_THRESHOLD` / `ITEM_CONFIRM_MID_THRESHOLD` |
| 9 | **TD-3/TD-4** 文档标记 | `docs/tech-debt-hardcoded-logic.md` | 标记为由 WS-6 统一意图架构隐式解决 |
| 10 | **P2-2** llm client 计量点 | `agent_core/llm/fallback.py` / `fallback_lc.py` / `agent_runtime/planner/protocol.py` / `execution_graph.py` | 计量源 `FallbackChatModel` 抽取 `usage_metadata`→`on_usage` 回调；`PlannerRuntime` 接线到 `ExecutionContext.record_usage`（contextvars 隔离）；`status` 事件带累计 `tokens_used`/`cost_used` |

### 文档更新

| 文件 | 更新内容 |
|------|----------|
| `docs/tech-debt-hardcoded-logic.md` | TD-3~TD-10 全部标记 ✅ 已修复 |
| `docs/runtime-governance-roadmap.md` | P2-1 / P4-2 / P4-3 标记 ✅ 已落地 |

### 前几轮已完成（上下文摘要）

- **WS-1~WS-8**（2026-08-20）：Agent 核心架构优化八工作流全量落地（MemoryStore 统一门面 / EventBus / KernelConfig / CircuitBreaker 并发安全 / FallbackChatModel 组合熔断 / GuardMiddleware / LLM LRU 缓存 / 意图分类器数据化）
- **Plan-F Phase 0~3**（2026-08-19）：Capability→Skill rename / 运行时模块迁入 agent-runtime / Planner 协议 / 组合治理 / WS 出口统一
- **架构审核落地**（2026-08-19）：PlannerRuntime per-request 隔离 / Skill 入参契约校验 / 术语精确化
- **文档更新**（2026-08-20）：CHANGELOG / ARCHITECTURE / AGENTS / README 等 6 个文档同步

## 三、验证状态

| 门禁 | 结果 |
|------|------|
| `ruff check .` | ✅ All checks passed |
| `scripts/lint_architecture.py` | ✅ P4-2 架构约束通过 |
| 根 pytest（排除 dialogue_framework/wenda_data_agent/wenda_adapter） | ✅ **484 passed** |
| 联邦 unit tests | ✅ **89 passed** |
| kefu tests | ✅ **8 passed** |
| eval（启发式） | 未在本轮运行（需 DATABASE_URL + Milvus 环境） |
| P2-2 单测 `tests/test_p2_2_usage_metering.py` | ✅ **10 passed**（usage 抽取 + 四路径外发 + runtime 接线 + status 带 tokens） |

> **注意**：`tests/dialogue_framework/` 和 `tests/wenda_data_agent/` 目录下的测试因模块尚未实现（`ModuleNotFoundError`）被排除，属预先存在问题，非本轮引入。

## 四、剩余待办（按优先级）

### 可执行但需评估

| # | 任务 | 来源 | 工作量 | 说明 |
|---|------|------|--------|------|
| 1 | ~~**P2-2** llm client 计量点~~ ✅ 已落地（2026-08-21） | `docs/runtime-governance-roadmap.md` | 0.5天 | 评估结论：底层 LangChain 客户端携带 `usage_metadata` 但 `FallbackChatModel` 未抽取，本项为真新代码；计量源抽取 + `PlannerRuntime` 接线 + `status` 带 tokens 均已实现 |
| 2 | **TD-1** kefu 意图路由验证 | `docs/tech-debt-hardcoded-logic.md` | — | 已标记 ✅ 但实际需验证 kefu 是否真正复用 `agent_core.intent` |

### 未来增强项（当前不阻塞）

| # | 任务 | 来源 | 工作量 | 阻塞原因 |
|---|------|------|--------|----------|
| 3 | **P3-1** Trajectory 持久化 | roadmap | 1.5天 | 依赖 P2-2（tokens 可后补时仅依赖 P1-1） |
| 4 | **P3-2** Trajectory Replay | roadmap | 2天 | 依赖 P3-1 |
| 5 | **P4-1** 分布式 session lease | roadmap | 3天 | 需多进程环境 + 双写一致性；当前单进程部署无暴露 |
| 6 | **P5-1** TrajectoryFingerprint | roadmap | 1.5天 | 增强项，现有结构检测已够用 |

### 预先存在的测试问题

- `tests/dialogue_framework/` 4 个测试文件 import 失败（模块未实现）
- `tests/wenda_data_agent/` 3 个测试文件 import 失败（模块未实现）
- 建议：要么实现对应模块，要么将这些测试移入各自 application 目录

## 五、关键文件索引

| 用途 | 路径 |
|------|------|
| 技术债清单 | `docs/tech-debt-hardcoded-logic.md`（TD-1~TD-14，全部已修复） |
| Runtime 路线图 | `docs/runtime-governance-roadmap.md`（P2-1~P5-1，P2-1/P4-2/P4-3 已落地） |
| 架构改进计划 | `docs/architecture-improvement-plan.md`（WS-1~WS-8 全量落地） |
| Plan-F 设计文档 | `docs/plan-f-single-runtime-multi-planner.md` |
| 架构约束 lint | `scripts/lint_architecture.py` |
| ExecutionContext（P2-1） | `packages/agent-runtime/agent_runtime/planner/protocol.py` |
| admission 常量（TD-10） | `packages/agent-runtime/agent_runtime/schemas.py` |
| 路由特征词数据（TD-7） | `applications/agent_server/agent/data/route_hints.json` |
| CHANGELOG | `CHANGELOG.md` |

## 六、建议的下一个 agent 执行顺序

1. ~~**P2-2**（llm client 计量点）~~ ✅ 已完成（见二/10 + 三）
2. **P3-1**（Trajectory 持久化）：在 P2-2 基础上添加存储层（trajectory `tokens` 字段现已有计量源，无需后补）
3. 处理预先存在的测试 import 失败问题
4. 运行 `make eval` 确认 eval 门禁在真实环境可达

## 七、建议 Skills

| Skill | 用途 |
|-------|------|
| `/code-review` | 对本轮所有变更做一次完整 code review |
| `/verification-before-completion` | 在声称任务完成前运行完整门禁验证 |
| `/implement` | 按 spec 实施 P2-2 / P3-1 等后续任务 |
| `/writing-plans` | 为 P3-1 Trajectory 持久化写实施计划 |
