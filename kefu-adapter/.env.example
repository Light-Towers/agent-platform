# kefu-adapter 环境变量示例
# 复制本文件为 .env 后按需修改；所有地址默认指向本地（localhost）。
# 真实密钥/内网地址请勿提交到仓库（.env 已被 .gitignore 忽略）。

# -------------------------- 下游老系统 --------------------------
# 外部 atguigu_ai 服务地址（不在本仓库内，为遗留系统）。
# kefu-adapter 经此转发 /api/messages 请求；删除 adapter 前不可置空。
KEFU_API_URL=http://localhost:5005

# -------------------------- 服务 --------------------------
# 本适配器监听端口（uvicorn 启动时可用 --port 覆盖）
PORT=8002
