# 掌柜智库（Zhanggui Zhiku）运行依赖
# 与 pyproject.toml 的 dependencies 保持一致；优先使用 `uv` 或 `pip install -e .`
# 若需锁定版本，可用 `pip freeze > requirements.txt` 覆盖本文件。

fastapi
flagembedding
grandalf
langchain
langchain-community
langchain-openai
langgraph
loguru
magic-pdf
minio
modelscope
numpy
pandas
pymilvus[model]
pymilvus-model
pymongo
python-dotenv
python-multipart
regex
torch
torchaudio
torchvision
uvicorn
transformers
openai-agents
requests
