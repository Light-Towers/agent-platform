# kefu-service 环境变量示例
# 复制本文件为 .env 后按需修改；所有地址默认指向本地（localhost）。
# 真实密钥/内网地址请勿提交到仓库（.env 已被 .gitignore 忽略）。
#
# 注意：本包（kefu-service）自身无独有环境变量，运行时配置全部继承自
# 共享内核 agent-core（LLM_* / EMBEDDING_* / DATABASE_URL 等，见 agent-core/.env.example
# 与 agent_federation/.env.example）。本文件仅作部署锚点，列出端口等运行参数。
# 迁移接入 `deepagents` PyPI 包网关前，需先将本服务升级为 Agent Protocol server
# 并补齐 QueryResponse 契约（见 README「已知待拍板项」U-2）。

# -------------------------- 服务 --------------------------
# 监听端口（启动命令：uvicorn kefu_agent:app --host 0.0.0.0 --port 8003）
PORT=8003
HOST=0.0.0.0

# -------------------------- 配置继承说明 --------------------------
# 下列变量由 agent-core / deepagents 提供，本包直接复用，无需在此重复定义：
#   DATABASE_URL         pgvector / postgres 连接串
#   LLM_PROVIDER / LLM_API_KEY / LLM_BASE_URL / LLM_MODEL
#   EMBEDDING_BACKEND / EMBEDDING_API_KEY / EMBEDDING_BASE_URL / EMBEDDING_MODEL
# 如确需覆盖，请在该包各自的 .env.example 中填值。
